{
  "name": "@makerdao/testchain",
  "version": "1.1.9",
  "license": "MIT",
  "files": [
    "out",
    "scripts",
    "snapshots",
    "yarn.lock"
  ],
  "dependencies": {
    "ganache-cli": "makerdao/ganache-cli#fix/gas-est-crash"
  },
  "scripts": {
    "deploy-mcd": "./scripts/launch -s scd-only -d --deploy-mcd-only -u",
    "create-default-snapshot": "./scripts/create-snapshot default --force"
  }
}
